# .streamlit/secrets.toml  — used when deployed on Streamlit Community Cloud
# Add this file's contents via the app dashboard → Settings → Secrets
# NEVER commit this file to GitHub (it's in .gitignore)

HF_TOKEN = "your_huggingface_token_here"
